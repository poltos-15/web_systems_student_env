paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    development:
        adapter: postgresql
        host: localhost
        name: webdb
        user: postgres
        pass: 'root'
        port: 3306
        charset: utf8